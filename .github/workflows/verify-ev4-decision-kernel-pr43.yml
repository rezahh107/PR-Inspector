name: Independently verify EV4 Decision Kernel PR 43

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verify-exact-target-head:
    name: Independent exact-head Coverage trust verification
    uses: rezahh107/PR-Inspector/.github/workflows/coverage-trust-gate.yml@15b587f6d8dc2cd22ba3a645ff48ddbba262a1da
    with:
      independent_policy_pr_number: 43
    permissions:
      contents: read
      pull-requests: read
      id-token: write
