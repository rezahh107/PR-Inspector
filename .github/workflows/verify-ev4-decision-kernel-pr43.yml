name: Independently verify EV4 Decision Kernel PR 43

on:
  pull_request:
  push:
    branches:
      - trust/coverage-external-gate-v1
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verify-exact-target-head:
    name: Independent exact-head Coverage trust verification
    uses: rezahh107/PR-Inspector/.github/workflows/coverage-trust-gate.yml@8b9b803a9f1e916f360d8871e43f5c48a11b5974
    permissions:
      contents: read
      pull-requests: read
      id-token: write
