name: Independently verify EV4 Decision Kernel PR 43

on:
  pull_request:
  push:
    branches:
      - trust/coverage-external-gate-v1
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verify-exact-target-head:
    name: Independent exact-head Coverage trust verification
    uses: rezahh107/PR-Inspector/.github/workflows/coverage-trust-gate.yml@adb91ee82f56e8592d8a852179a84eb5b654116f
    with:
      independent_policy_pr_number: 43
    permissions:
      contents: read
      pull-requests: read
      id-token: write
