name: Independently verify EV4 Decision Kernel PR 43

on:
  pull_request:
  push:
    branches:
      - trust/coverage-external-gate-v1
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verify-exact-target-head:
    name: Independent exact-head Coverage trust verification
    uses: rezahh107/PR-Inspector/.github/workflows/coverage-trust-gate.yml@de320aef2f88b01fa3273b221d2518965903c9cc
    with:
      independent_policy_pr_number: 43
    permissions:
      contents: read
      pull-requests: read
      id-token: write
