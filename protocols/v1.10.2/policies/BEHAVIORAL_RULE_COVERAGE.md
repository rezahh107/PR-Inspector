# Behavioral Rule Coverage — Dual-Audience Decision Projection

Version: `v1.10.2`  
Scope: canonical decision projection, authoritative action text, owner readiness, next-action routing, artifact integrity, CI identity, and provenance-bound re-review sequencing.  
Decision: `PASS` only while repository validation and the focused mutation suite pass on the exact active head.

This matrix applies Behavioral Rule Coverage v0.4.1. It tracks only Critical/High behavior. A prose statement, schema field, or advisory workflow is not counted as enforcement. Each Critical per-artifact rule below is exercised by a dedicated mutation in CI. Additional adversarial tests cover contradictory legacy action prose; forged inspector identity; missing review artifacts; mismatched hashes; wrong PR/head; stale validity; replay; orphan acceptance without a pending repair; and non-acceptable canonical decisions. The cross-turn re-review rule is replayed as a structured sequence with externally verified provenance and is therefore recorded as `sequence_ci_enforced`, not merely `ci_enforced`.

| rule_id | concept | risk | prose_source | schema_carrier | validator_rule | valid_fixture | invalid_fixture | CI_step | downstream_contract | session_scope | recovery_action | status |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `PRR-OWNER-MERGE-001` | Merge-now owner wording requires current technical Green, no pending approval, and no pending action. | Critical | `protocols/v1.10.2/policies/OWNER_OUTPUT_UX.md#merge-now` | `DECISION_PROJECTION.json#/owner_readiness` | `project_decision` and `validate_projection_invariants`; trigger owner rendering; predicate action is merge_now only with CURRENT, Green, and no approval; enforcement reject projection | `fixtures/golden-green/review-package.json` | `fixtures/behavioral-rules/mutation-cases.json#owner_merge_with_pending_approval` | python -m pytest -q tests/test_behavioral_rule_coverage.py | `validate_directory` rejects divergent projection or owner artifact | per_artifact | block | ci_enforced |
| `PRR-OWNER-TWO-LINE-001` | Direct owner result is exactly two UTF-8 LF-terminated Persian lines with one status icon. | Critical | `protocols/v1.10.2/policies/OWNER_OUTPUT_UX.md#two-line-contract` | `OWNER_RESULT.fa.txt` and owner message registry | `validate_directory`; trigger artifact validation; predicate bytes equal registered two-line output; enforcement reject artifact set | `fixtures/golden-green/review-package.json` | `fixtures/behavioral-rules/mutation-cases.json#owner_result_third_line` | python -m pytest -q tests/test_behavioral_rule_coverage.py | `validate_directory` rejects byte mismatch before downstream use | per_artifact | block | ci_enforced |
| `PRR-DECISION-SOURCE-001` | All derived artifacts, including Technical Handoff action text, consume one canonical decision projection; legacy action prose is non-authoritative. | Critical | `protocols/v1.10.2/PR_REVIEW_CONTRACT.md#decide-once` | `DECISION_PROJECTION.json` and projection-keyed canonical action text | `project_decision`, `render_handoff`, and `PRI-PROJECTION-003`; trigger projection and handoff validation; predicate action kind, recipient, authority, and action text derive from projection while legacy prose is omitted as instruction; enforcement reject drift | `fixtures/golden-green/review-package.json` | `fixtures/behavioral-rules/mutation-cases.json#projection_action_drift` | python -m pytest -q tests/test_behavioral_rule_coverage.py | semantic validation, rendering, and artifact validation import or consume the same projection | per_artifact | block | ci_enforced |
| `PRR-REASON-REGISTRY-001` | Every status and action reason is registered and maps once to effect, recipient, authority, prompt kind, and recovery. | Critical | `protocols/v1.10.2/policies/DECISION_GATES.md#reason-registry` | `registries/DECISION_REASON_REGISTRY.yaml` and projection reason-code arrays | `reason_registry_entries` and `PRI-PROJECTION-004`; trigger load and projection validation; predicate every code is registered with canonical routing; enforcement fail closed | `fixtures/golden-green/review-package.json` | `fixtures/behavioral-rules/mutation-cases.json#unregistered_reason_code` | python -m pytest -q tests/test_behavioral_rule_coverage.py | repository validation rejects invalid registry and artifact validation rejects unknown codes | per_artifact | block | ci_enforced |
| `PRR-VERIFY-NOMODIFY-001` | Verification-only actions cannot authorize patching, commits, refactors, or behavior changes. | Critical | `protocols/v1.10.2/policies/DERIVED_OUTPUTS.md#verify` | `DECISION_PROJECTION.json#/next_action/may_modify_code` and verification prompt | `validate_projection_invariants` plus deterministic prompt comparison; trigger verify routing; predicate may_modify_code false and no repair-authority text; enforcement reject projection or prompt | `fixtures/golden-green/review-package.json` with unresolved required check mutation in test | `fixtures/behavioral-rules/mutation-cases.json#verify_repair_authority` | python -m pytest -q tests/test_behavioral_rule_coverage.py | reviewer recipient must stop and request a fresh decision when repair becomes necessary | per_artifact | block | ci_enforced |
| `PRR-STALE-NOREPAIR-001` | Stale or unknown review identity routes only to fresh review and cannot authorize repair. | Critical | `protocols/v1.10.2/policies/DECISION_GATES.md#validity` | `review_identity.review_validity` and `DECISION_PROJECTION.json#/next_action` | `project_decision` and `PRI-PROJECTION-003`; trigger non-current validity; predicate action rerun_review, reviewer recipient, may_modify_code false; enforcement reject drift | `fixtures/repair-handoff-valid/review-package.json` with STALE mutation in test | `fixtures/behavioral-rules/mutation-cases.json#stale_routes_to_repair` | python -m pytest -q tests/test_behavioral_rule_coverage.py | fresh-review prompt treats previous findings as non-authorizing historical context | per_artifact | block | ci_enforced |
| `PRR-RECIPIENT-BOUNDARY-001` | A model prompt cannot replace mandatory human technical or specialist approval. | Critical | `protocols/v1.10.2/policies/SECURITY_AND_TRUST.md#human-control` | `DECISION_PROJECTION.json#/next_action/recipient` and human-review handoff | `validate_projection_invariants` plus deterministic handoff comparison; trigger approval routing; predicate recipient matches approval and may_modify_code false; enforcement reject drift | `fixtures/golden-green/review-package.json` with specialist approval mutation in test | `fixtures/behavioral-rules/mutation-cases.json#specialist_routes_to_implementer` | python -m pytest -q tests/test_behavioral_rule_coverage.py | handoff states generated model output cannot satisfy or claim the approval | per_artifact | block | ci_enforced |
| `PRR-MANIFEST-BYTES-001` | Manifest hashes equal SHA-256 of the actual final artifact bytes on disk. | Critical | `protocols/v1.10.2/policies/DERIVED_OUTPUTS.md#artifact-byte-integrity` | `artifact-manifest.json` entries with `hash_scope: final_file_bytes` | `_manifest_diagnostics` and `PRI-MANIFEST-003`; trigger directory validation; predicate reread file hash equals manifest; enforcement reject artifact set | rendered `fixtures/repair-handoff-valid/review-package.json` | `fixtures/behavioral-rules/mutation-cases.json#artifact_byte_changed` | python -m pytest -q tests/test_behavioral_rule_coverage.py | review-directory validator blocks altered bytes, CRLF, BOM, or newline drift | per_artifact | block | ci_enforced |
| `PRR-CI-IDENTITY-001` | CI evidence records tested ref type, SHA, tree, reviewed head, synthetic-merge state, run, and jobs. | Critical | `protocols/v1.10.2/policies/DERIVED_OUTPUTS.md#ci-identity` | `ci-identity.schema.json` and CI identity record | `validate_ci_identity`; trigger CI evidence acceptance; predicate identity fields are complete and consistent; enforcement reject evidence object | `fixtures/ci-identity/exact-head.json` | `fixtures/behavioral-rules/mutation-cases.json#synthetic_merge_claimed_exact` | python -m pytest -q tests/test_behavioral_rule_coverage.py | exact-head workflow emits and validates a structured identity record | per_artifact | block | ci_enforced |
| `PRR-EXACT-HEAD-CLAIM-001` | Exact-head claims require tested SHA equal reviewed head and synthetic_merge false. | Critical | `protocols/v1.10.2/policies/DERIVED_OUTPUTS.md#exact-head-claims` | CI identity `claim_exact_head`, `tested_sha`, `reviewed_head_sha`, and `synthetic_merge` | `validate_ci_identity` with `PRI-CI-IDENTITY-003`; trigger exact-head claim; predicate tested object is PR head and SHA equality holds; enforcement reject claim | `fixtures/ci-identity/exact-head.json` | `fixtures/behavioral-rules/mutation-cases.json#merge_sha_substituted_for_head` | python -m pytest -q tests/test_behavioral_rule_coverage.py | workflow separately asserts `git rev-parse HEAD` equals triggering PR head | per_artifact | block | ci_enforced |
| `PRR-PENDING-REREVIEW-001` | Acceptance after repair requires a preceding pending-repair event plus immutable validated review artifacts and authoritative GitHub inspector-commit evidence bound to the same repository, PR, repaired head, protocol, package, projection, manifest, and canonical decision. | Critical | `protocols/v1.10.2/PR_REVIEW_CONTRACT.md#re-review-provenance-boundary` | schema-v2 lifecycle event plus opaque `VerifiedReviewEvidence`; locked `INSPECTOR_TRUST_POLICY.json`; canonical package/projection/manifest hashes | `verify_github_commit_payload`, `verify_review_directory`, `validate_rereview_sequence`, and `PRI-SEQUENCE-001` through `PRI-SEQUENCE-010`; trigger lifecycle replay; predicate orphan acceptance, self-asserted PASSED/identity/hashes, and unmatched evidence never unlock; enforcement fail closed | `fixtures/rereview-sequence/valid.json` bound to rendered golden-green evidence in test | `fixtures/behavioral-rules/mutation-cases.json#accept_without_verified_rereview` | python -m pytest -q tests/test_behavioral_rule_coverage.py | operational adapter retrieves live GitHub repository/commit evidence before sequence evaluation; external consumers remain outside this PR | cross_turn | rollback | sequence_ci_enforced |
| `PRR-COV-AUTHORITY-001` | External Coverage trust identity requires OIDC issuer/audience, authenticated caller repository/workflow, GitHub API repository/PR/base/head agreement, positive run ID/attempt agreement, immutable issuer SHA, and proof-credit denial. | Critical | `protocols/v1.10.2/policies/EXTERNAL_COVERAGE_TRUST.md#required-invariants` | OIDC claims, event payload, API PR payload, bootstrap attestation | `derive_authoritative_identity`, `issue_bootstrap_attestation`, and `verify_attestation`; reject authority mismatch, caller-selected identity, stale head, wrong issuer, missing API evidence, and proof credit | `tests/test_coverage_trust_gate.py#IdentityTests` | `fixtures/behavioral-rules/mutation-cases.json#coverage_authority_mismatch` | python -m pytest -q tests/test_coverage_trust_gate.py | exact identity and no-proof attestation are emitted only after authority agreement | per_artifact | block | ci_enforced |
| `PRR-COV-WORKFLOW-001` | Target Coverage workflow topology is parsed structurally with duplicate-key rejection and requires exact reusable pin, no disabling if, dependency, exact checkout ref, exact two-step validation topology, clean/reset/status validation command, and live validation-step identity env. | Critical | `protocols/v1.10.2/policies/EXTERNAL_COVERAGE_TRUST.md#required-invariants` | target workflow YAML structure | `load_workflow_yaml` and `workflow_diagnostics`; comments, malformed YAML, anchors, non-mapping structures, duplicate keys, wrong-step bindings, stale pins, forbidden identity inputs, arbitrary run steps, mutable execution context, async execution, and failure suppression fail closed | `tests/test_coverage_trust_gate.py#TopologyTests` | `fixtures/behavioral-rules/mutation-cases.json#coverage_workflow_duplicate_key` | python -m pytest -q tests/test_coverage_trust_gate.py | malformed or ambiguous workflow topology cannot satisfy external trust | per_artifact | block | ci_enforced |
| `PRR-COV-SUPPLY-001` | External Coverage workflow permissions, supply chain, and execution-content identity are least privilege: exact permissions, full action SHAs, approved checkout SHA, persist-credentials false, exact externally verified checkout ref, clean worktree attestation precondition, and no PATH/NODE_OPTIONS/GITHUB_ENV/BASH_ENV drift. | Critical | `protocols/v1.10.2/policies/EXTERNAL_COVERAGE_TRUST.md#required-invariants` | target workflow YAML permissions, steps, action uses, checkout with mapping | `workflow_diagnostics`; broader permissions, mutable action refs, malformed checkout with, missing or true persist-credentials, wrong checkout action SHA, dirty worktrees, and environment/path injection fail closed | `tests/test_coverage_trust_gate.py#TopologyTests` | `fixtures/behavioral-rules/mutation-cases.json#coverage_supply_chain_mutable_action` | python -m pytest -q tests/test_coverage_trust_gate.py | target validation cannot run with broadened permissions or mutable supply-chain references | per_artifact | block | ci_enforced |
| `PRR-COV-PLANNING-001` | Coverage planning tree is complete and readable before attestation; malformed, unreadable, invalid-UTF-8 JSON and any proof-credit request block bootstrap. | Critical | `protocols/v1.10.2/policies/EXTERNAL_COVERAGE_TRUST.md#required-invariants` | `planning/coverage/**/*.json` and bootstrap attestation | `planning_coverage_diagnostics` and `evaluate_target`; partial planning trees and proof-credit claims fail closed with bounded diagnostics | `tests/test_coverage_trust_gate.py#PlanningCoverageTests` | `fixtures/behavioral-rules/mutation-cases.json#coverage_planning_malformed_json` | python -m pytest -q tests/test_coverage_trust_gate.py | no completed attestation is issued from partial planning evidence | per_artifact | block | ci_enforced |
| `PRR-COV-INTEGRATION-001` | External Coverage integration evidence requires an already canonical resolvable issuer commit, exact-head GitHub Actions success, manual PR43 trigger scope only after release, and artifact identity bound to current target head and same issuer SHA. | High | `protocols/v1.10.2/policies/EXTERNAL_COVERAGE_TRUST.md#required-invariants` | GitHub commit endpoint evidence, workflow run/job IDs, attestation artifact identity | release workflow and manual integration handoff; unresolved issuer pins or absent exact-head CI keep integration blocked and cannot prove closure | `tests/test_coverage_trust_gate.py#TopologyTests` | `fixtures/behavioral-rules/mutation-cases.json#coverage_integration_unresolved_issuer` | python -m pytest -q tests/test_coverage_trust_gate.py | integration success is not claimed until official GitHub evidence resolves the issuer and exact target head | cross_turn | flag_for_review | fixture_tested |
| `PRR-PROMPT-INJECTION-001` | Package and repository free text remains serialized untrusted data and cannot create instruction sections. | Critical | `protocols/v1.10.2/policies/SECURITY_AND_TRUST.md#prompt-injection-containment` | JSON-serialized package-derived fields inside action artifact | deterministic prompt renderer and exact artifact comparison; trigger prompt rendering; predicate free text is encoded after trust boundary; enforcement reject mutated prompt | `fixtures/repair-handoff-valid/review-package.json` | `fixtures/behavioral-rules/mutation-cases.json#prompt_section_injection` | python -m pytest -q tests/test_behavioral_rule_coverage.py | action recipient is explicitly instructed not to execute embedded target content | per_artifact | block | ci_enforced |
| `PRR-GOV-PERSONAL-PROFILE-001` | Personal minimum security requires sequence CI or exact-bound verified repository enforcement before Green. | Critical | `protocols/v1.10.2/policies/PERSONAL_MINIMUM_SECURITY_PROFILE.md#default-minimum` | `review-package.schema.json#/properties/security_profile` and `decision-projection.schema.json#/properties/security_profile` | `assess_security_profile` plus `project_decision`; missing minimum emits registered Yellow verification reason | `fixtures/golden-green/review-package.json` | `fixtures/behavioral-rules/mutation-cases.json#profile_minimum_enforcement_absent` | python -m pytest -q tests/test_behavioral_rule_coverage.py | owner result, handoff, and prompt are derived from the same projection | per_artifact | block | ci_enforced |
| `PRR-GOV-PROFILE-TRIGGER-001` | Explicit repository, security, external, or evidence triggers require verified repository-hosted enforcement. | Critical | `protocols/v1.10.2/policies/PERSONAL_MINIMUM_SECURITY_PROFILE.md#evidence-boundary` | structured activation booleans and governance evidence identifier | exact-bound opaque governance capability verification; absent evidence emits registered Yellow verification reason | `fixtures/governance/personal-minimum-security-cases.json#explicit_higher_security_activation` | `fixtures/behavioral-rules/mutation-cases.json#profile_trigger_without_verified_enforcement` | python -m pytest -q tests/test_behavioral_rule_coverage.py | activation cannot be satisfied by prose or booleans | per_artifact | block | ci_enforced |
| `PRR-GOV-CLAIM-SEPARATION-001` | Repository-settings enforcement and merge authorization remain separate claims and require verifier-created evidence. | Critical | `protocols/v1.10.2/policies/PERSONAL_MINIMUM_SECURITY_PROFILE.md#evidence-boundary` | structured claim booleans, evidence ID, and sealed `VerifiedGovernanceEvidence` | self-asserted claims become rejected and emit registered reasons; exact-bound opaque evidence is required for verified | `fixtures/governance/personal-minimum-security-cases.json#verified_governance_evidence` | `fixtures/behavioral-rules/mutation-cases.json#profile_self_asserted_stronger_claims` | python -m pytest -q tests/test_behavioral_rule_coverage.py | serialized lookalikes cannot mint governance capability | per_artifact | block | ci_enforced |
| `PRR-GOV-PROJECTION-AUTHORITY-001` | Helper-only or manually injected profile output cannot alter the official canonical decision. | Critical | `protocols/v1.10.2/policies/PERSONAL_MINIMUM_SECURITY_PROFILE.md#scope` | canonical decision projection and final artifact manifest | `project_decision`, projection equality validation, manifest byte validation | rendered `fixtures/golden-green/review-package.json` | `fixtures/behavioral-rules/mutation-cases.json#profile_manual_projection_override` | python -m pytest -q tests/test_behavioral_rule_coverage.py | altered projection or helper output is rejected and cannot reroute owner/action output | per_artifact | block | ci_enforced |
| `PRR-OWNER-PROMPT-ATOMIC-001` | Prompt-required owner output is delivered atomically; compact access cannot omit the required prompt. | Critical | `protocols/v1.10.2/PR_REVIEW_CONTRACT.md#atomic-owner-delivery-contract` | `OWNER_DELIVERY_CONTRACT.json`, `DECISION_PROJECTION.json#/next_action/prompt_required`, `OWNER_RESULT.fa.txt`, and conditional `NEXT_ACTION_PROMPT.en.md` | `official_owner_delivery` and `official_owner_result`; trigger prompt-required verified completion; predicate compact access raises `CompletionError` while atomic access returns exact verified bytes; enforcement fail closed before owner text | `fixtures/repair-handoff-valid/review-package.json` | `fixtures/behavioral-rules/mutation-cases.json#compact_owner_result_with_required_prompt` | python -m pytest -q tests/test_behavioral_rule_coverage.py | supported CLI writes only complete verified owner delivery and leaves stdout empty on delivery failure | per_artifact | block | ci_enforced |

## Enforcement interpretation

- The focused CI command executes the dedicated mutations and the contradictory-action, forged-inspector, missing-artifact, mismatched-hash, identity-binding, replay, orphan-acceptance, and non-acceptable-decision adversarial cases; any violation fails CI.
- `sequence_ci_enforced` is used only for `PRR-PENDING-REREVIEW-001`, because that rule is validated across ordered lifecycle events and verified artifact provenance. `PRR-COV-INTEGRATION-001` remains `fixture_tested` until fresh GitHub Actions and manual/downstream evidence exist.
- No row claims `downstream_contract_enforced`; the repository proves producer-side and included-adapter rejection while external consumers remain outside this PR.
- Real cryptographic-signature, production, OS-harness, or independent downstream enforcement is not claimed.
